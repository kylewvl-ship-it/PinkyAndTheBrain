# Claude Handoff Prompt

Codex will replace this file with the next Claude task.

Run:

```powershell
.\scripts\invoke-claude-handoff.cmd
```

Then return control to Codex with `_bmad-output/agent-handoff/claude-result.md`.
